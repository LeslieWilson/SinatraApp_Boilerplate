post '/articles' do
    if @urls.include?(params[:url])
        @error_message = "you have already submitted that URL"
        erb :new
    else
        CSV.open("articles.csv", "a") do |csv|
            csv << [array_of_article_objects.length + 1, params[:name], params[:url], "#{params[:description]}"]
        end
    end
